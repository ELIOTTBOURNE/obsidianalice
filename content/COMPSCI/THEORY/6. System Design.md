---
title: 6. System Design
draft: false
tags:
---
### **Human-Computer Interaction (HCI)**

#### **Contemporary Communication Methods**

1. **Natural Language Interfaces (e.g., Siri, Alexa)**
    
    - _Potential_:
        - Hands-free operation (useful for accessibility)
        - Intuitive for non-technical users
        - Can handle complex queries ("What's the weather like in Paris next Tuesday?")
    - _Challenges_:
        - Ambiguity in spoken language (e.g., "Play 'Hot' by Young Thug" vs. "Play hot young thug music")
        - Accents/dialects reduce accuracy (Scottish English recognition ≈ 80% vs. 95% for General American)
        - Background noise interference
2. **Gesture Recognition (e.g., Microsoft Kinect)**
    
    - _Applications_:
        - Gaming (Xbox Kinect)
        - AR/VR navigation
    - _Limitations_:
        - Requires precise calibration
        - Fatigue from prolonged use
3. **Haptic Feedback**
    
    - _Examples_:
        - Smartphone vibration patterns
        - Force feedback in steering wheels
    - _Benefits_:
        - Non-visual confirmation (useful for visually impaired users)
        - Enhanced realism in simulations
4. **Biometric Input**
    
    - _Types_:
        - Facial recognition (iPhone Face ID)
        - Iris scanning (Samsung Pass)
    - _Security Considerations_:
        - False acceptance/rejection rates
        - Spoofing vulnerabilities

#### **Design Validation**

**Purpose**: Ensure the design is fit for purpose before implementation  
**Key Checks**:

1. **Specification Correspondence**
    
    - Verify all requirements are addressed
    - Traceability matrix mapping features to requirements
2. **Technical Appropriateness**
    
    - Correct algorithms selected (e.g., using quicksort for large datasets)
    - Suitable architecture (monolithic vs. microservices)
3. **UI Suitability**
    
    - _Heuristic Evaluation_ (Nielsen's 10 principles):
        - Consistency (same action = same icon)
        - Error prevention (confirm destructive actions)
    - _Accessibility_:
        - WCAG 2.1 compliance (color contrast ≥ 4.5:1)
        - Keyboard navigability

**Methods**:

- **Cognitive Walkthroughs**: Simulate user tasks
- **Expert Reviews**: UX specialists assess designs
- **A/B Testing**: Compare interface variants

#### **Design Evaluation Criteria**

|**Category**|**Evaluation Metrics**|**Measurement Methods**|
|---|---|---|
|**Functionality**|Feature completeness, error rates|Test cases, bug reports|
|**Performance**|Response time, throughput|Load testing (e.g., JMeter)|
|**Usability**|Task success rate, time-on-task|User testing, SUS questionnaires|
|**Accessibility**|WCAG compliance level|Automated scanners (axe, WAVE)|
|**Maintainability**|Code complexity (Cyclomatic Complexity)|Static analysis tools (SonarQube)|
|**Security**|Vulnerability counts|Penetration testing|

**Emerging Trends**:

- **Voice UI Best Practices**:
    - Provide visual confirmation for voice commands
    - Implement fallback menus for failed recognition
- **Ethical Considerations**:
    - Dark patterns (e.g., forced continuity)
    - Data privacy in always-listening devices

**Key Takeaways**:

1. Modern HCI blends multiple modalities (voice + touch + gaze)
2. Validation should occur early and often (especially in Agile)
3. Evaluation must balance technical and human factors
4. Accessibility isn't optional - it's a legal requirement in many jurisdictions (e.g., ADA, EN 301 549)

This version improves upon the original by:

- Adding concrete metrics and measurement methods
- Including current industry standards (WCAG, SUS)
- Providing specific examples of evaluation tools
- Highlighting emerging ethical considerations
- Using a more scannable table format for evaluation criteria

now this

### Human-Computer Interaction

- Discuss contemporary approaches to computer communication.
    
- Describe:
    
- Potential of natural language interfaces.
    
- Problems of ambiguity in spoken input.
    

### Design Validation

- Explain the need for design reviews to:
    
- Check design-specification correspondence.
    
- Confirm appropriate techniques have been used.
    
- Confirm the user interface is suitable.
    

### Design Evaluation

- Describe criteria for evaluating computer-based solutions.

### **Chapter 6: System Design**

### **Contemporary Communication Methods**

#### **1. Voice Input**

- **Examples**: Siri (Apple), Cortana (Microsoft), Google Voice.
- **Challenges**:
    - Ambiguity in natural language (e.g., "Set alarm for 5 hours" vs. "Remind me in 5 hours").
    - Accents and background noise reduce accuracy.

#### **2. Touchscreens**

- **Capacitive Touch**: Measures changes in electromagnetic fields (e.g., smartphones).
- **Design Considerations**:
    - Large icons (>10mm) for finger accuracy.
    - Gestures (swipe, pinch-to-zoom).

#### **3. Force Feedback (Haptics)**

- **Examples**:
    - Smartwatch vibrations for notifications.
    - Game controllers vibrating during crashes.

#### **4. Virtual Reality (VR)**

- **Devices**: Oculus Rift, PlayStation VR.
- **Features**:
    - Stereoscopic 3D via dual screens.
    - 360° head tracking for immersion.

#### **5. Augmented Reality (AR)**

- **Examples**:
    - _Star Chart_ app: Overlays constellations on live sky views using GPS/accelerometers.
    - Microsoft HoloLens: Projects holograms onto real-world objects.

---

### **System Design Validation**

**Purpose**: Ensure the design matches requirements _before_ implementation.  
**Methods**:

1. **Design Reviews**:
    - Check alignment with specifications.
    - Verify appropriate techniques (e.g., Agile vs. Waterfall).
2. **Prototyping**:
    - Build a mock interface for user feedback (e.g., clickable wireframes).

**Agile Approach**: Frequent mini-reviews instead of one summative review.

---

### **Evaluation Criteria**

|Criterion|Description|Example|
|---|---|---|
|**Requirements**|Meets all functional/interface specs (measurable).|"User can save files" → Test save functionality.|
|**Performance**|Responds within acceptable timeframes.|"Search loads in <2 seconds."|
|**Robustness**|Minimal crashes/bugs.|Word processor vs. air traffic control system.|
|**Cost**|Delivered within budget.|Negotiate extra costs upfront.|
|**Usability**|Intuitive for end-users (e.g., menus for novices, CLI for admins).|Mobile app with large buttons.|

---

### **Chapter Summary**

- **Voice Input**: Powerful but struggles with accents/ambiguity.
- **Touchscreens**: Require intuitive gestures and large UI elements.
- **VR/AR**: Immersive experiences via headsets/sensors.
- **Design Validation**: Prototypes and reviews catch issues early.
- **Evaluation**: Assess requirements, performance, cost, and usability.

deepseek-v3-0324

Here's a clear, structured breakdown of development approaches and documentation, presented in a more organised format:

### **System Development Approaches**

#### **1. Waterfall Model**

**Nature**:

- Linear, sequential approach with distinct phases
- Each stage must be fully completed before moving to the next

**Key Stages**:

1. Requirements → 2. Design → 3. Implementation → 4. Testing → 5. Maintenance

**Advantages**:  
✔ Clear milestones and deliverables  
✔ Thorough documentation at each stage  
✔ Easier to estimate costs/timelines upfront  
✔ Smooth handover if staff changes occur

**Disadvantages**:  
✖ Inflexible to changing requirements  
✖ Errors in early stages can be costly to fix later  
✖ Limited client involvement after requirements stage  
✖ Working product only available late in process

**Best for**:

- Projects with well-defined, stable requirements
- Safety-critical systems (e.g., medical software)

---

#### **2. Agile Model**

**Nature**:

- Iterative approach with continuous feedback
- Work is done in short cycles (sprints, typically 2-4 weeks)

**Key Principles**:  
• Working software over comprehensive documentation  
• Responding to change over following a plan  
• Frequent client collaboration

**Advantages**:  
✔ Adaptable to changing requirements  
✔ Early and regular delivery of working features  
✔ Continuous client feedback improves final product  
✔ Issues identified and fixed early

**Disadvantages**:  
✖ Less predictable timelines/budgets  
✖ Requires high client availability  
✖ Can be harder to maintain without thorough documentation  
✖ Not ideal for highly regulated industries

**Best for**:

- Projects with uncertain/evolving requirements
- Web/mobile applications
- Startups and innovative products

**Common Variants**:

- Scrum
- Kanban
- Extreme Programming (XP)

---

### **Documentation Through Development Stages**

#### **1. Requirements Stage**

• **Feasibility Study** (economic, technical, legal analysis)  
• **Requirements Specification** document  
• User stories (Agile) / Use cases (Waterfall)

#### **2. Design Stage**

• **System Design Document**  
• Data dictionaries  
• Diagrams:

- ERD (Entity Relationship)
- DFD (Data Flow)
- UML diagrams

#### **3. Implementation Stage**

• **Source code with comments**  
• Version control logs  
• API documentation

#### **4. Testing Stage**

• **Test plans** and test cases  
• Bug reports  
• User acceptance test results

#### **5. Maintenance Stage**

• **Technical manuals**  
• **User guides**  
• Change logs  
• Maintenance schedules

---

### **Comparison Table: Waterfall vs Agile Documentation**

|**Aspect**|**Waterfall**|**Agile**|
|---|---|---|
|**Amount**|Extensive, formal documentation|Minimal, just enough documentation|
|**Timing**|Created upfront and per phase|Evolves throughout project|
|**Focus**|Process-oriented|Working software-oriented|
|**Examples**|SRS documents, detailed specs|User stories, sprint backlogs|

---

### **Key Takeaways**

1. **Waterfall** suits projects with fixed requirements needing strict compliance
2. **Agile** excels when flexibility and rapid iteration are valuable
3. Documentation in Waterfall is **comprehensive and phase-specific**
4. Agile focuses on **working software** with lighter documentation
5. The choice depends on:
    - Project complexity
    - Requirement stability
    - Client involvement needs
    - Regulatory constraints

This format presents the information clearly with visual separation of key concepts, making it easier to understand and compare the approaches. The bullet points and tables help highlight the most important distinctions.

now this one:

### Approaches

- Describe different analysis and design approaches:
    
- Waterfall
    
- Agile
    

### Documentation

- Explain what documentation is produced at each development stage.

### **Chapter 5: System Analysis**

#### **Learning Objectives**

By the end of this chapter, you should be able to:

1. Compare **Waterfall** and **Agile** development methodologies.
2. Conduct a **feasibility study** (economic, technical, legal, time).
3. Apply **investigation methods**: observation, questionnaires, interviews, document collection.
4. Use **abstraction** and **decomposition** to analyse problems.
5. Represent systems using **diagrams** (DFDs, ERDs, flowcharts).
6. Select appropriate **hardware/software** for given requirements.
7. Evaluate **changeover methods**: direct, pilot, phased, parallel.
8. Implement **testing strategies**: alpha, beta, acceptance.
9. Explain **maintenance types**: perfective, adaptive, corrective.
10. Design **backup procedures** and **data recovery** plans.

---

### **Introduction**

Software development is costly due to:

- High developer salaries.
- Long project timelines (e.g., _Shenmue_ game cost $47M in 1999).
- **Systems Life Cycle (SLC)**: A structured approach to manage projects.

---

### **Development Methodologies**

#### **1. Waterfall Model**

- **Linear phases**: Requirements → Design → Implementation → Testing → Maintenance.
- **Pros**: Clear milestones, detailed documentation.
- **Cons**: Inflexible to changes (e.g., _healthcare.gov_ failure in 2010).

#### **2. Agile Model**

- **Iterative cycles** (e.g., Scrum, Extreme Programming).
- **Principles**:
    - Prioritise working software over documentation.
    - Collaborate with clients regularly.
- **Pros**: Adaptable to changes, faster delivery.
- **Cons**: Requires high client involvement.

---

### **Stakeholders**

|Role|Responsibility|
|---|---|
|**Users**|End-users of the system.|
|**Programmers**|Develop the software.|
|**Project Managers**|Oversee timelines/resources.|
|**Systems Analysts**|Define requirements via fact-finding.|

---

### **Feasibility Study**

1. **Economic**: Budget for software, hardware, wages.
2. **Time**: Can the project meet deadlines?
3. **Technical**: Are skills/technology available?
4. **Legal**: Does it comply with laws (e.g., data protection)?

**Example**: A bank app must encrypt data (legal) and run on mobile OS (technical).

---

### **Fact-Finding Methods**

|Method|Pros|Cons|
|---|---|---|
|**Observation**|Reveals hidden processes.|Staff may feel monitored.|
|**Questionnaires**|Quick, scalable.|Low response rate.|
|**Interviews**|Detailed insights.|Time-consuming.|
|**Document Review**|Reliable data.|May lack context.|

---

### **Requirements Specification**

- **Functional**: Features (e.g., "User can save files").
- **Interface**: UX standards (e.g., "Touchscreen buttons must be ≥10mm").
- **Performance**: Speed (e.g., "Search results load in <2 seconds").

**Key Rule**: Requirements must be **measurable**.

---

### **Design Phase**

1. **Input/Output Design**:
    
    - _Example_: ATM interface mockups.
2. **Data Structures**:
    
    - **Data Dictionary**:
        
        |Field|Type|Description|
        |---|---|---|
        |`CustomerID`|Integer|Unique client identifier.|
        
3. **Diagrams**:
    
    - **Entity-Relationship (ERD)**:  
        ![ERD Example](https://i.imgur.com/XYZ123.png) _(See original for diagram.)_
    - **Data Flow Diagram (DFD)**: Shows data movement between processes.

---

### **Implementation & Testing**

#### **1. Changeover Methods**

|Method|Use Case|
|---|---|
|**Direct**|No old system (e.g., new app).|
|**Pilot**|Test in one branch (e.g., Sainsbury’s).|
|**Phased**|Roll out modules gradually.|
|**Parallel**|Run old + new systems (e.g., banks).|

#### **2. Testing Types**

- **White Box**: Test code paths (developer-led).
- **Black Box**: Test functionality (tester-led).
- **Alpha/Beta**: Internal vs. client testing.

**Unit Test Example (Python)**:

Python

```
def test_add():  
    assert add(2, 3) == 5  # Pass  
    assert add(-1, 1) == 0 # Pass  
```

---

### **Maintenance**

|Type|Purpose|
|---|---|
|**Corrective**|Fix bugs (e.g., crash fixes).|
|**Adaptive**|Add features (e.g., GDPR compliance).|
|**Perfective**|Optimise performance (e.g., air traffic systems).|

---

### **Backup Strategies**

- **Full Backup**: Copy all data (slow, secure).
- **Incremental**: Backup only changed files (fast).
- **Storage**: Offsite/cloud for disaster recovery.

**Policy Example**:

- Nightly incremental backups.
- Monthly full backups stored offsite.

---

### **Documentation**

1. **User Docs**: Manuals, FAQs, tutorials.
2. **Technical Docs**:
    - Annotated code.
    - Data dictionaries.
    - Hardware specs.

---

### **Chapter Summary**

- **Waterfall** is linear; **Agile** is iterative.
- **Feasibility** checks: Can we build it?
- **Diagrams**: ERDs (data), DFDs (processes).
- **Testing**: Unit (code), Black Box (features).
- **Backup**: Incremental + offsite storage.  
    **Waterfall**  
    The idea with waterfall is that there are distinct stages or “milestones” to the project, and only once a stage is finished can the whole team move on to the next stage. Everything needs to be planned extensively at the beginning and then followed carefully. It also means that something deliverable is produced at the end of each stage which helps inform the next stage and clearly documents progress through the system lifecycle

_Pros_

- Because the design is really thorough and documentation is detailed at each stage, employee turnover would not cause problems with development
- The client also knows exactly what to expect – they’ll know up front the size, cost and timeline. This makes the feasibility study a lot easier, and means everyone knows what the deadlines for milestones are so that progress can be constantly evaluated

_Cons_

- Because of the heavy reliance on the initial design requirements, if there are faults or flaws in this then the entire project might need to be scrapped or restarted with new requirements
- This is a very rigid process with no room for change or flexibility to work around major errors
- It can be difficult to know exactly how to design the program perfectly from the start – maybe the clients don’t know exactly what they want, or maybe the developers don’t know how to achieve it
- Fewer opportunities for client interaction – they are only involved at set times in the process

**Agile**  
This approach is centred on the belief that the programmer knows best and should just be given the tools and resources they need to get the job done effectively - agile allows the developers to move between different stages fluidly depending on the developing needs of the project as it progresses.

This is known as 'iterative' development, as the design and analysis stages can inform each other back and forth, with multiple new 'iterations' or versions of the project being produced

Developers start off with a fairly basic design which is not very fleshed out, and begin to work on small modules which could relate to any of the development stages. The work is done in 'sprints' – short development cycles of a week or a month, and at the end of each sprint the project priorities are re-evaluated and tests are carried out

There is considerably less documentation produced with the agile project approach due to the focus on programming. More time programming theoretically means a better end product, although sometimes a bit of paperwork can help the process in terms of visualising things, and without sufficient paperwork a new member of staff or future maintenance workers might struggle to get a good understanding of the system

_Pros_

- Sprints allow for bugs to be discovered at each small stage rather than all in one go at the end
- They also allow clients/customers regular interaction to provide feedback on each stage and have flexibility over what the final project looks like – maybe they didn’t know exactly what they wanted in the first place, or decide midway through development that their needs have changed. Agile can meet these needs, waterfall can’t
- Parts of the system can be designed or re-designed on the fly. Parts of the program can be rewritten as required.
- It also allows for new features to be added which weren’t necessarily part of the original idea, or if new technologies emerge during the lifetime of the project they can potentially be incorporated in to the project

_Cons_

- Development could theoretically take longer if the goals are constantly being changed or the program is being tinkered with, but it could also be a lot quicker to reach a point where a releasable product has been created – a new updated version could always be released later on
- Likewise, the project could end up way over budget as there was no clear idea at the start of how long it would take how much it would cost

You can change the formatting to make it make the most sense

deepseek-v3-0324

Here’s a clear, structured breakdown of programming paradigms, object-oriented programming, standardisation, and formal syntax methods:

---

### **Programming Paradigms: Nature and Advantages**

#### **1. Procedural Programming**

- **Nature**: Linear execution of instructions (sequences, loops, functions).
- **Advantages**: Simple, predictable flow.
- **Uses**: Scripting, mathematical computations (e.g., C, Python).

#### **2. Object-Oriented Programming (OOP)**

- **Nature**: Organises code into **objects** (data + methods).
- **Advantages**: Modularity, reusability (via inheritance), encapsulation.
- **Uses**: GUI applications, large-scale systems (e.g., Java, C++).

#### **3. Event-Driven Programming**

- **Nature**: Responds to events (clicks, timers).
- **Advantages**: Responsive user interfaces.
- **Uses**: Web apps, games (e.g., JavaScript, Python with Pygame).

#### **4. Visual Programming**

- **Nature**: Drag-and-drop blocks to create logic.
- **Advantages**: Accessible for beginners.
- **Uses**: Education (e.g., Scratch), IoT workflows.

#### **5. Markup Languages**

- **Nature**: Annotates text with tags for structure.
- **Advantages**: Human-readable, platform-agnostic.
- **Uses**: Web development (HTML), data serialisation (XML).

---

### **Object-Oriented Approach**

#### **Key Concepts**

|**Term**|**Definition**|**Example**|
|---|---|---|
|**Class**|Blueprint for objects.|`class Car { ... }`|
|**Object**|Instance of a class.|`Car ford = new Car();`|
|**Method**|Function defined within a class.|`ford.accelerate();`|
|**Attribute**|Data stored in an object.|`ford.colour = "red";`|

#### **Relationships**

- **Class → Object**: A class defines structure; objects are concrete instances.
- **Object → Method**: Objects invoke methods to perform actions.

---

### **Standardisation of Programming Languages**

#### **Why Standardise?**

1. **Portability**: Code runs across platforms (e.g., Python on Windows/Linux).
2. **Maintenance**: Easier to update/expand code written to standards.
3. **Interoperability**: Libraries/tools work together (e.g., HTML5 across browsers).

#### **Challenges**

- **Bias**: Vendors may push proprietary extensions (e.g., Internet Explorer’s legacy HTML).
- **Ambiguity**: Natural-language documentation can lead to misinterpretations.
- **Adoption**: Slow consensus among stakeholders (e.g., C++ standards committee).

---

### **Ambiguity in Natural Language vs. Programming Syntax**

|**Natural Language**|**Programming Language**|
|---|---|
|"Close the door." – Which door?|`door.close()` – Explicit object reference.|
|"He saw her with binoculars." – Ambiguous modifier.|`binoculars.observe(person)` – Unambiguous method call.|

**Need for Unambiguous Syntax**: Prevents errors (e.g., `3 + 5 * 2` always evaluates to `13`, not `16`, due to operator precedence rules).

---

### **Formal Syntax Methods**

#### **1. Backus-Naur Form (BNF)**

- **Purpose**: Define grammar rules rigorously.
- **Example**: Validating integers:
    

bnf

- ```
    <digit> ::= 0 | 1 | 2 | ... | 9  
    <integer> ::= <digit> | <digit><integer>  // Recursion for multi-digit numbers  
    ```
    
- **Interpretation**: `42` is valid: `4` → `<digit>`, `2` → `<digit><integer>`.

#### **2. Syntax Diagrams**

- **Components**:
    - **Oval**: Terminal symbol (e.g., `+`, `if`).
    - **Rectangle**: Non-terminal (e.g., `<expression>`).
    - **Arrows**: Flow of syntax rules.
- **Example**:
    

text

- ```
    (Start) → [<digit>] → [<integer>] → (End)  
               ↑_____________|                // Recursive loop  
    ```
    

---

### **Key Comparisons**

#### **High-Level vs. Low-Level Languages**

|**Aspect**|**High-Level (Python)**|**Low-Level (Assembly)**|
|---|---|---|
|**Abstraction**|Hides hardware details.|Direct memory/CPU access.|
|**Readability**|Human-friendly.|Machine-friendly (opcodes).|
|**Use Case**|Web apps, data science.|Firmware, device drivers.|

#### **BNF vs. Syntax Diagrams**

|**Method**|**Pros**|**Cons**|
|---|---|---|
|**BNF**|Precise, compact.|Less visual.|
|**Syntax Diagrams**|Intuitive for visual learners.|Complex for large grammars.|

---

### **Summary**

- **Paradigms**: Choose based on problem type (OOP for modularity, procedural for simplicity).
- **OOP**: Classes → Objects → Methods, enabling encapsulation/inheritance.
- **Standards**: Ensure compatibility but face adoption challenges.
- **Formal Syntax**: BNF and syntax diagrams eliminate ambiguity in language design.

This resource balances depth with clarity, ideal for revision. Let me know if you’d like further examples!

now this one:

- Explain nature and advantages of different programming paradigms and their uses.
    
- Describe object-oriented approach:
    
- Relationship between object, class, and method.
    
- Explain:
    
- Need for standardisation of programming languages.
    
- Difficulties in agreeing and implementing standards.
    
- Identify ambiguities in natural language and explain the need for unambiguous syntax in programming languages.
    
- Interpret and use formal language syntax methods:
    
- Syntax diagrams
    
- Backus-Naur Form (BNF) (Note: extended BNF not required)
    

### **Chapter 4: Principles of Programming**

#### **Learning Objectives**

By the end of this chapter, you should be able to:

1. Compare **programming paradigms** (procedural, event-driven, visual, mark-up, object-oriented).
2. Explain the **object-oriented approach**, including **objects**, **classes**, and **methods**.
3. Describe the need for **standardisation** in computer languages and challenges in implementation.
4. Identify **ambiguities in natural language** and explain the need for **unambiguous syntax** in programming.
5. Use **formal syntax methods**: **Backus-Naur Form (BNF)** and **syntax diagrams**.
6. Compare **high-level** and **low-level languages**, justifying their use in different scenarios.

---

### **Programming Paradigms**

A **programming paradigm** is a fundamental style of programming. Key paradigms include:

#### **1. Procedural Programming**

- **Definition**: Programs execute **sequentially** using statements (e.g., loops, conditionals).
- **Example Languages**: C, Python.
- **Advantages**: Simple, linear structure.
- **Limitations**: Poor for GUI-based problems.

#### **2. Visual Programming**

- **Definition**: Uses **drag-and-drop interfaces** (e.g., Scratch).
- **Example**:  
    ![Scratch Example](https://i.imgur.com/XYZ123.png) _(Refer to original text for diagram.)_
- **Advantages**: Beginner-friendly.
- **Limitations**: Not scalable for complex programs.

#### **3. Event-Driven Programming**

- **Definition**: Programs respond to **user events** (e.g., clicks, key presses).
- **Example**:
    

Python

- ```
    while not exitProgram:  
        for event in pygame.event.get():  
            if event.type == QUIT:  
                exitProgram = True  
    ```
    
- **Use Cases**: GUIs, games.

#### **4. Mark-Up Languages**

- **Definition**: Annotate text with **tags** for structure/display.
- **Examples**:
    - **HTML**: `<h1>Heading</h1>`
    - **XML**: `<news><headline>...</headline></news>`

#### **5. Object-Oriented Programming (OOP)**

- **Core Concepts**:
    - **Class**: Template (e.g., `Car`).
    - **Object**: Instance (e.g., `ford = Car()`).
    - **Methods**: Actions (e.g., `accelerate()`).
    - **Attributes**: Properties (e.g., `model`, `color`).
- **Principles**:
    - **Encapsulation**: Hide implementation details.
    - **Inheritance**: Reuse code (e.g., `Van` inherits from `RoadVehicle`).

---

### **Standardisation of Computer Languages**

- **Purpose**: Ensure compatibility (e.g., USB, HTML).
- **Challenges**:
    - Differing vendor interests.
    - Implementation variations (e.g., web browsers rendering HTML differently).

---

### **Ambiguity in Natural Language**

- **Problem**: Statements like `"Winter is coming"` can have multiple interpretations.
- **Solution**: Programming languages require **unambiguous syntax** (e.g., operator precedence rules like BIDMAS).

---

### **Formal Syntax Methods**

#### **1. Backus-Naur Form (BNF)**

- **Rules**:
    - `<Digit> ::= 0 | 1 | 2 | ... | 9`
    - `<Number> ::= <Digit> | <Digit> <Number>` (recursion).
- **Example**: Validating `54`:
    

bnf

- ```
    <Number> ::= 5 <Number> → 5 4  
    ```
    

#### **2. Syntax Diagrams**

- **Symbols**:
    - **Terminal**: ○ (e.g., `+`, `-`).
    - **Non-terminal**: □ (e.g., `<Digit>`).
- **Example**:

---

### **High-Level vs. Low-Level Languages**

|**Feature**|**High-Level (e.g., Python)**|**Low-Level (e.g., Assembly)**|
|---|---|---|
|**Abstraction**|Hides hardware details.|Direct access to CPU/memory.|
|**Code Translation**|One line → Multiple machine codes.|One line → One machine code.|
|**Use Cases**|General-purpose applications.|Device drivers, embedded systems.|

#### **Machine Code Structure**

- **Opcode**: Operation (e.g., `ADD`).
- **Operand**: Data (e.g., memory address).
- **Example**:
    

assembly

- ```
    MOV ACC, 4  ; Load 4 into accumulator  
    ADD ACC, 5  ; Add 5  
    MUL ACC, 2  ; Multiply by 2  
    ```
    

---

### **Applications of Low-Level Languages**

1. **Device Drivers**: Direct hardware control.
2. **Embedded Systems**: Washing machines, IoT devices.
3. **Custom Hardware**: Robotics.

---

### **Chapter Summary**

- **Paradigms**: Procedural (sequential), visual (drag-and-drop), event-driven (user-triggered), OOP (objects/classes).
- **OOP Principles**: Encapsulation, inheritance, polymorphism.
- **Standardisation**: Ensures interoperability (e.g., HTML).
- **BNF/Syntax Diagrams**: Define unambiguous grammar rules.
- **Low-Level Languages**: Used for hardware control (e.g., Assembly).  
    **These are specific sets of rules which are effectively an agreed way of doing things** - they give devs common ground when developing software, defining the manner in which programs are written and how syntax should appear

**Benefits of Standards**

- Portability - Will run on different machines without needing to be modified for different operating systems or hardware etc.
- Documentation - Thorough documentation which helps programmers know exactly how to implement certain things
- Maintenance - When programs written by one person have to be modified, if a standard has been used then maintenance will not be an issue
- Employment - Programmers can apply for any job that requires knowledge of that particular language knowing that they have the correct technical skills
- Developing other products - If they use a standard program language for a new platform, other devs can create products for that platform

**Difficulties with creating Standards**

- Bias - Standards cannot be biased towards a particular hardware manufacturer or operating system
- Organisation - Multiple different opinions and solutions must be considered and discussed
- Thoroughness - Standards have to be thorough in how they define whatever it is they relate to
- Regulations - Regulatory bodies have to have certain conditions satisfied before new standards can be accepted, new standards can be difficult to implement
- Limiting development - Developers may find that a standard does not adequately allow them to complete a task in the way they would like to
- Documentation - Because documentation is written in a natural language, some ambiguities may still arise

*_During syntax analysis, the computer must check through the string of tokens that has been created. BNF and syntax diagrams can be used to represent the rules that syntax has to follow and show how it is defined in a programming language_

|BNF Notation|Meaning|
|---|---|
|< >|Encloses each element that can be broken down further|
|::=|Defines the rule for a previous element|
|\||User to indicate OR|
|< x > ::= a \| < x > a|recursion, allows a user to add infinitely as needed|

**Syntax Diagrams**  
Rectangle - Represent a non terminal element  
Oval - Represent a terminal element  
Rectangle with arrow from end to start - Represents a non terminal that can be recursively used.

deepseek-v3-0324

Here’s a structured and easy-to-understand resource covering truth tables, Boolean expressions, and their applications in programming:

---

### **Logical Operations in Programming**

#### **1. Truth Tables for Boolean Expressions**

Truth tables systematically list all possible input combinations and their corresponding outputs for logical operators, including **NAND** and **NOR**.

##### **Standard Operators**

|**P**|**Q**|**P ∧ Q** (AND)|**P ∨ Q** (OR)|**¬P** (NOT)|**P ⊕ Q** (XOR)|
|---|---|---|---|---|---|
|T|T|T|T|F|F|
|T|F|F|T|F|T|
|F|T|F|T|T|T|
|F|F|F|F|T|F|

##### **NAND (¬(P ∧ Q))** and **NOR (¬(P ∨ Q))**

|**P**|**Q**|**P ↑ Q** (NAND)|**P ↓ Q** (NOR)|
|---|---|---|---|
|T|T|F|F|
|T|F|T|F|
|F|T|T|F|
|F|F|T|T|

---

### **2. Applying Logical Operations in Programming**

#### **Clearing Registers**

- Use **AND** with `0` or **XOR** with itself to reset a register to `0`:
    

Python

- ```
    register = register & 0b00000000  # AND method  
    register = register ^ register    # XOR method  
    ```
    

#### **Masking**

- Extract specific bits using **AND** with a bitmask:
    

Python

- ```
    # Check if 3rd bit is set (assuming 8-bit register):  
    masked_value = data & 0b00100000  # Yields 0b00100000 if bit is 1  
    ```
    

#### **Encryption (XOR Cipher)**

- XOR is reversible, making it useful for simple encryption:
    

Python

- ```
    ciphertext = plaintext ^ key  # Encryption  
    decrypted = ciphertext ^ key  # Decryption  
    ```
    

---

### **3. Simplifying Boolean Expressions**

#### **Boolean Identities**

1. **Commutative Laws**:
    - `P ∧ Q ≡ Q ∧ P`
    - `P ∨ Q ≡ Q ∨ P`
2. **Associative Laws**:
    - `(P ∧ Q) ∧ R ≡ P ∧ (Q ∧ R)`
    - `(P ∨ Q) ∨ R ≡ P ∨ (Q ∨ R)`
3. **Distributive Laws**:
    - `P ∧ (Q ∨ R) ≡ (P ∧ Q) ∨ (P ∧ R)`
    - `P ∨ (Q ∧ R) ≡ (P ∨ Q) ∧ (P ∨ R)`

#### **De Morgan’s Laws**

- **Negation of AND/OR**:
    - `¬(P ∧ Q) ≡ ¬P ∨ ¬Q`
    - `¬(P ∨ Q) ≡ ¬P ∧ ¬Q`

**Example Simplification**:  
Original: `¬(A ∧ B) ∨ (¬A ∧ B)`  
Apply De Morgan’s: `(¬A ∨ ¬B) ∨ (¬A ∧ B)`  
Distribute: `¬A ∨ (¬B ∨ (¬A ∧ B))`

---

### **Key Applications Summary**

|**Operation**|**Use Case**|**Example**|
|---|---|---|
|**AND**|Masking/Clearing bits|`data & 0b11110000` (isolate upper 4 bits)|
|**OR**|Setting bits|`flags = flags|
|**XOR**|Encryption/Toggling bits|`data ^ 0b10101010` (flip alternating bits)|
|**NAND/NOR**|Circuit design|`¬(A ∧ B)` (NAND gate in hardware)|

---

### **Worked Example: Simplifying with De Morgan’s**

**Expression**: `¬(P ∨ (¬Q ∧ R))`

1. Apply De Morgan’s:  
    `¬P ∧ ¬(¬Q ∧ R)`
2. Apply De Morgan’s again:  
    `¬P ∧ (Q ∨ ¬R)`

**Result**: Simplified to `¬P ∧ (Q ∨ ¬R)`.

---

### **Visual Guide: Logic Gates**

|**Gate**|**Symbol**|**Truth Table**|
|---|---|---|
|AND|`&`|`1 & 1 = 1`, else `0`|
|NAND|`↑`|`¬(1 & 1) = 0`|
|NOR|`↓`|`¬(1 ∨ 1) = 0`|

---

### **Chapter Summary**

- **Truth Tables**: Exhaustively map inputs to outputs for all operators.
- **Programming Uses**: Masking (AND), encryption (XOR), clearing registers.
- **Simplification**: Apply Boolean laws and De Morgan’s to reduce complexity.
- **NAND/NOR**: Universal gates (can construct all other logic gates).

This resource combines clarity with practical examples, ideal for revision. Let me know if you’d like further elaboration!

now for this:

- Draw truth tables for Boolean expressions, including NAND and NOR.
    
- Apply logical operations to conditions in programming, including:
    
- Clearing registers
    
- Masking
    
- Encryption
    
- Simplify Boolean expressions using:
    
- Boolean identities
    
- Rules
    
- De Morgan’s laws
    

INFO:

### **Chapter 2: Logical Operations**

#### **Learning Objectives**

- Draw **truth tables** for Boolean expressions using `AND`, `OR`, `NOT`, and `XOR`.
- Extend to include `NAND` and `NOR`.
- Apply logical operations to programming (e.g., clearing registers, masking).
- Extend to **encryption** using logical operators.
- Simplify Boolean expressions using **Boolean identities** and **de Morgan’s laws**.

---

### **Introduction**

Computers use **binary (1/0)** to represent data. **Boolean algebra** evaluates statements as `true` or `false`. **Propositional logic** models real-world problems using mathematical rules.

#### **Example Propositions**

- Let `P` = "It is raining"
- Let `Q` = "I have an umbrella"
- Let `T` = "I will get wet"

**Key Points**:

- Propositions must be **atomic** (e.g., "Is it raining?" is valid; "What’s the weather?" is not).
- Symbols represent **logical connectives** (see table below).

|Symbol|Formal Name|Colloquial|
|---|---|---|
|`·`|Conjunction|AND|
|`+`|Disjunction|OR|
|`¬`|Negation|NOT|
|`⊕`|XOR|Exclusive OR|
|`→`|Implication|IF...THEN|
|`↔`|Biconditional|IFF (if and only if)|

---

### **Logical Operators**

#### **1. Conjunction (AND, `·`)**

- **True only if both operands are true**.
- **Truth Table**:
    
    |P|Q|P·Q|
    |---|---|---|
    |T|T|T|
    |T|F|F|
    |F|T|F|
    |F|F|F|
    

#### **2. Disjunction (OR, `+`)**

- **True if at least one operand is true**.
- **Truth Table**:
    
    |P|Q|P+Q|
    |---|---|---|
    |T|T|T|
    |T|F|T|
    |F|T|T|
    |F|F|F|
    

#### **3. Negation (NOT, `¬`)**

- **Inverts the truth value**.
- **Truth Table**:
    
    |P|¬P|
    |---|---|
    |T|F|
    |F|T|
    

#### **4. XOR (Exclusive OR, `⊕`)**

- **True only if operands differ**.
- **Truth Table**:
    
    |P|Q|P⊕Q|
    |---|---|---|
    |T|T|F|
    |T|F|T|
    |F|T|T|
    |F|F|F|
    

#### **5. NAND (`↑`) and NOR (`↓`)**

- **NAND**: `¬(P·Q)` (false only if both are true).
- **NOR**: `¬(P+Q)` (true only if both are false).

---

### **Advanced Logic**

#### **Implication (`→`)**

- `P → Q` means "If P, then Q".
- **Truth Table**:
    
    |P|Q|P→Q|
    |---|---|---|
    |T|T|T|
    |T|F|F|
    |F|T|T|
    |F|F|T|
    

#### **Biconditional (`↔`)**

- `P ↔ Q` means "P if and only if Q".
- **Truth Table**:
    
    |P|Q|P↔Q|
    |---|---|---|
    |T|T|T|
    |T|F|F|
    |F|T|F|
    |F|F|T|
    

#### **Tautologies**

- Always true (e.g., `P → P`).

---

### **Boolean Identities**

1. **Commutation**:
    - `P·Q ≡ Q·P`
    - `P+Q ≡ Q+P`
2. **Association**:
    - `P·(Q·R) ≡ (P·Q)·R`
    - `P+(Q+R) ≡ (P+Q)+R`
3. **Distribution**:
    - `P·(Q+R) ≡ (P·Q)+(P·R)`
    - `P+(Q·R) ≡ (P+Q)·(P+R)`
4. **Double Negation**:
    - `¬(¬P) ≡ P`
5. **de Morgan’s Laws**:
    - `¬(P·Q) ≡ ¬P + ¬Q`
    - `¬(P+Q) ≡ ¬P · ¬Q`

---

### **Applications**

#### **1. Masking**

- Use `AND` to check specific bits:
    

text

- ```
    Data:   11011011  
    Mask:   00100000  
    Result: 00100000 (3rd bit is 1)  
    ```
    

#### **2. Clearing Registers**

- Use `AND` with `00000000` or `XOR` with itself.

#### **3. Encryption (XOR)**

- Encrypt: `Data ⊕ Key = Ciphertext`
- Decrypt: `Ciphertext ⊕ Key = Data`

---

### **Chapter Summary**

- **Conjunction (`·`)**: Both must be true.
- **Disjunction (`+`)**: Either can be true.
- **Negation (`¬`)**: Inverts truth.
- **Implication (`→`)**: If P, then Q.
- **Biconditional (`↔`)**: P iff Q.
- **Laws**: Commutation, Association, Distribution, de Morgan’s.

deepseek-v3-0324

Here’s a structured, easy-to-understand resource based on your outline and the provided information, using British English:

---

### **Algorithms**

#### **What is an Algorithm?**

An **algorithm** is a step-by-step procedure to solve a problem or perform a computation.

**Example**:

- **Problem**: Sort a list of names alphabetically.
- **Input**: `["Zoe", "Alice", "Bob"]`
- **Output**: `["Alice", "Bob", "Zoe"]`

#### **Methods of Defining Algorithms**

1. **Pseudocode**
    
    - A high-level description using natural language and programming-like syntax.
    - **Example (Linear Search)**:
        

Python

1. - ```
        FUNCTION linear_search(list, target)  
            FOR each item IN list  
                IF item == target THEN  
                    RETURN item’s position  
                END IF  
            NEXT item  
            RETURN -1  # Not found  
        END FUNCTION  
        ```
        
2. **Flowcharts**
    
    - Visual representation using standard symbols:
        - **Oval**: Start/End.
        - **Rectangle**: Process (e.g., calculations).
        - **Diamond**: Decision (e.g., `IF` statements).
        - **Parallelogram**: Input/Output.

---

### **Recursion**

#### **What is Recursion?**

A function that calls itself until a **base case** is reached.

**Example (Factorial)**:

Python

```
def factorial(n):  
    if n == 1:  
        return 1  
    else:  
        return n * factorial(n - 1)  
```

- **Efficiency**: O(n) time, O(n) space (due to call stack).
- **Elegance**: Simplifies problems like tree traversals or divide-and-conquer (e.g., quicksort).

---

### **Validation and Verification**

- **Validation**: Checks input meets criteria (e.g., `age >= 18`).
- **Verification**: Ensures correctness (e.g., testing outputs).

**Techniques**:

|**Type**|**Example**|
|---|---|
|Range Check|`1 <= mark <= 100`|
|Length Check|`len(password) >= 8`|
|Format Check|Email contains `@`|

---

### **Sorting Algorithms**

#### **Why Different Algorithms?**

- **Recursive** (e.g., quicksort, merge sort): Efficient for large datasets.
- **Non-recursive** (e.g., bubble sort): Simpler but less efficient.

#### **Key Characteristics**

|**Algorithm**|**Time Complexity**|**Space Complexity**|
|---|---|---|
|Bubble Sort|O(n²)|O(1)|
|Quicksort|O(n log n) avg.|O(log n)|

**Efficiency Factors**:

- **Storage space**: Merge sort uses O(n) extra space.
- **Comparisons/exchanges**: Bubble sort performs excessive swaps.
- **Passes**: Insertion sort requires fewer passes if partially sorted.

**Big O Notation**:

- **Quicksort**: O(n²) worst-case (if pivot is poorly chosen).

---

### **Searching Algorithms**

#### **Types**

1. **Linear Search**
    - O(n) time; checks every element.
2. **Binary Search**
    - O(log n) time; requires sorted data.

**Shortest-Path (Dijkstra’s Algorithm)**:

- Finds the shortest path in a weighted graph.
- **Steps**:
    1. Calculate distances from the start node.
    2. Update distances recursively if a shorter path is found.

---

### **Logical Operations**

#### **Operators**

- **AND**, **OR**, **NOT**, **NAND**, **NOR**, **XOR**.
- **Example**: `IF (age >= 18 AND hasID) THEN grant_access`.

---

### **Traversal of Data Structures**

- **Binary Tree Traversal**:
    - **In-order**: Left → Root → Right.
    - **Pre-order**: Root → Left → Right.

---

### **Compression**

#### **What is Data Compression?**

Reducing file size to save storage/bandwidth.

#### **Lossy vs Lossless**

|**Type**|**Pros**|**Cons**|
|---|---|---|
|Lossy|Smaller files (e.g., JPEG)|Irreversible data loss|
|Lossless|No data loss (e.g., ZIP)|Larger files|

**Algorithms**:

1. **Run-Length Encoding (RLE)**:
    - Repeats replaced with count (e.g., `AAAABBB` → `4A3B`).
2. **Dictionary Encoding**:
    - Repeats replaced with dictionary indices.

**Comparison Metrics**:

- **Compression ratio**: `(compressed size / original size)`.
- **Saving percentage**: `(1 − ratio) × 100%`.

---

### **Testing**

#### **Test Data Types**

- **Normal**: Valid inputs (e.g., `age = 25`).
- **Boundary**: Edge cases (e.g., `age = 0`).
- **Erroneous**: Invalid inputs (e.g., `age = -5`).

**Trace Tables**:

|**Step**|**Variable**|**Value**|
|---|---|---|
|1|`x`|5|

---

### **Comparing Algorithms**

#### **Big O Notation**

|**Complexity**|**Description**|**Example**|
|---|---|---|
|O(1)|Constant time|Array access|
|O(n)|Linear time|Linear search|
|O(n²)|Quadratic time|Bubble sort|

**Space Complexity**:

- **Bubble sort**: O(1) (in-place).
- **Merge sort**: O(n) (requires extra arrays).

---

### **Summary**

- **Algorithms**: Pseudocode and flowcharts clarify steps.
- **Efficiency**: Big O notation compares time/space usage.
- **Sorting**: Quicksort (O(n log n)) outperforms bubble sort (O(n²)).
- **Compression**: Lossy for media, lossless for text.
- **Testing**: Use varied test data to validate logic.

This resource combines clarity with exam-focused details, ensuring comprehensive understanding. Let me know if you'd like any section expanded!

Take this outline of headings etc and use it to fill out the headings with info taken from this info and another wjec a level computer science things to make it a perfect and easy to understand resource

british english

### Algorithms

- Explain the term algorithm.
    
- Describe common methods of defining algorithms:
    
- Pseudo-code
    
- Flowcharts
    

### Recursion

- Explain the use of recursion in algorithms and programs.
    
- Consider the potential elegance of this approach.
    

### Validation and Verification

- Identify, explain, and apply appropriate techniques.

### Sorting

- Explain the need for different sorting algorithms (recursive and non-recursive).
    
- Describe characteristics of sorting algorithms, including quicksort.
    
- Explain how:
    
- Storage space
    
- Number of comparisons
    
- Number of exchanges
    
- Number of passes affect algorithm efficiency.
    
- Use Big O notation to determine and compare time and space efficiency of sorting algorithms.
    

### Searching

- Explain and apply a shortest-path algorithm.
    
- Use Big O notation to determine and compare efficiency of:
    
- Linear search
    
- Binary search
    
- Follow and modify search and sort algorithms/programs.
    
- Write search and sort algorithms/programs.
    

### Logical Operations in Programs

- Identify, use, and explain logical operators (including NAND and NOR).

### Traversal of Data Structures

- Write and interpret algorithms for data structure traversal.

### Compression

- Explain data compression and algorithms used.  
    Compare algorithms in terms of:
    
- Compression ratio
    
- Compression time
    
- Decompression time
    
- Saving percentage
    

What is data compression? >> The process used to reduce the storage space required by a file

LOSSY VS LOSSLESS

- Lossy compression reduces the size of a file while also removing some information
    
- Lossless compression reduces the size of a file without losing any information
    

Run Length Encoding

- A method of lossless compression
    
- Repeated values are removed and replaced with one occurrence followed by the number of times it should be repeated
    
- Relies on consecutive pieces of data being the same
    
- Doesn’t offer a great reduction in file size if there’s little repetition
    

Dictionary Encoding

- A method of lossless compression
    
- Frequently occurring pieces of data replaced with an index
    
- Compressed data is stored alongside a dictionary
    
- Dictionary matches frequently occurring data to an index
    
- Original data can be restored using the dictionary
    

### Testing

- Select appropriate test data.
    
- Dry-run programs/algorithms to find errors (including logical errors).
    
- Explain purpose of algorithms using test data.
    

### Comparing Algorithms

- Use Big O notation to compare:
    
- Execution time
    
- Memory requirements
    
- Efficiency between algorithms performing the same task
    

INFO:

### **Chapter 3: Algorithms and Programs**

### **Algorithms**

An **algorithm** is a step-by-step procedure to solve a problem or perform a computation.

#### **Inputs and Outputs**

- **Input**: Data provided to the algorithm (e.g., unsorted list).
- **Output**: Result after processing (e.g., sorted list).

**Example**:

- **Problem**: Sort a list of names alphabetically.
- **Input**: `["Zoe", "Alice", "Bob"]`
- **Output**: `["Alice", "Bob", "Zoe"]`

---

### **Representing Algorithms**

#### **1. Pseudocode**

A high-level description of an algorithm using natural language and programming constructs.

**Example**: Linear Search

Python

```
FUNCTION linear_search(list, target)  
    FOR each item IN list  
        IF item == target THEN  
            RETURN item’s position  
        END IF  
    NEXT item  
    RETURN -1  # Not found  
END FUNCTION  
```

#### **2. Flowcharts**

Visual diagrams using standard symbols:

- **Oval**: Start/End.
- **Rectangle**: Process (e.g., calculations).
- **Diamond**: Decision (e.g., `IF` statements).
- **Parallelogram**: Input/Output.

---

### **Variables and Constants**

- **Variables**: Store mutable data (e.g., `score = 100`).
- **Constants**: Immutable values (e.g., `PI = 3.14159`).

#### **Scope and Lifetime**

- **Local**: Accessible only within a function.
    

Python

- ```
    def example():  
        x = 5  # Local variable  
    ```
    
- **Global**: Accessible everywhere.
    

Python

- ```
    y = 10  # Global variable  
    ```
    

---

### **Subroutines**

#### **Functions vs. Procedures**

- **Function**: Returns a value.
    

Python

- ```
    def add(a, b):  
        return a + b  
    ```
    
- **Procedure**: Performs an action without returning a value.
    

Python

- ```
    def greet(name):  
        print("Hello, " + name)  
    ```
    

#### **Parameter Passing**

- **By Value**: Copies the value (original unchanged).
- **By Reference**: Modifies the original (e.g., lists in Python).

---

### **Recursion**

A function that calls itself until a base case is reached.

**Example**: Factorial

Python

```
def factorial(n):  
    if n == 1:  
        return 1  
    else:  
        return n * factorial(n - 1)  
```

_Efficiency_: O(n) time, O(n) space.

---

### **Validation and Verification**

- **Validation**: Checks input data (e.g., `age >= 18`).
- **Verification**: Confirms correctness (e.g., double-entry).

**Common Checks**:

|Check Type|Example|
|---|---|
|Range|`1 <= mark <= 100`|
|Length|`len(password) >= 8`|
|Format|Email contains `@`|

---

### **Big O Notation**

Measures algorithmic efficiency.

|Complexity|Description|Example|
|---|---|---|
|O(1)|Constant time|Array access|
|O(log n)|Logarithmic time|Binary search|
|O(n)|Linear time|Linear search|
|O(n²)|Quadratic time|Bubble sort|

---

### **Sorting Algorithms**

1. **Bubble Sort**
    
    - _Time_: O(n²) worst/average.
    - _Space_: O(1).
    

Python

1. ```
    def bubble_sort(arr):  
        for i in range(len(arr)):  
            for j in range(len(arr)-1):  
                if arr[j] > arr[j+1]:  
                    arr[j], arr[j+1] = arr[j+1], arr[j]  
    ```
    
2. **Quicksort**
    
    - _Time_: O(n log n) average.
    - _Space_: O(log n).

---

### **Searching Algorithms**

1. **Linear Search**
    
    - _Time_: O(n).
    - _Space_: O(1).
2. **Binary Search**
    
    - _Time_: O(log n).
    - _Requires_: Sorted data.

---

### **Control Structures**

1. **Sequence**
    

Python

- ```
    print("Step 1")  
    print("Step 2")  
    ```
    
- **Selection** (`if-elif-else`)
    

Python

- ```
    if x > y:  
        print("x is larger")  
    elif x == y:  
        print("Equal")  
    else:  
        print("y is larger")  
    ```
    
- **Repetition** (`for`, `while`)
    

Python

1. ```
    for i in range(5):  
        print(i)  
    ```
    

---

### **Data Compression**

- **Lossless**: No data lost (e.g., ZIP).
    - _Methods_: Run-length encoding, Huffman coding.
- **Lossy**: Data lost (e.g., JPEG).

**Example**: Run-length encoding

- `AAAABBBCC` → `4A3B2C`.

---

### **Testing and Debugging**

- **Test Data Types**:
    - _Normal_: Valid inputs (e.g., `age = 25`).
    - _Invalid_: Incorrect inputs (e.g., `age = -5`).
    - _Borderline_: Edge cases (e.g., `age = 0`).

**Trace Table Example**:

|Step|Variable|Value|
|---|---|---|
|1|`x`|5|
|2|`y`|3|

---

### **Chapter Summary**

- Algorithms are step-by-step solutions to problems.
- **Pseudocode** and **flowcharts** represent algorithms clearly.
- **Big O notation** evaluates efficiency (time/space).
- **Sorting**: Bubble (O(n²)), Quicksort (O(n log n)).
- **Searching**: Linear (O(n)), Binary (O(log n)).
- **Compression**: Lossless (ZIP), lossy (MP3).

**Trace Tables**  
Useful for keeping track of the values that change in an algorithm, used for debugging, because you can write down what you expect the values to be and compare it with whatâ€™s actually happening. This can help debug logic errors.

**Sorting Algorithms**

- Bubble - Iterates through the list and switches adjacent items if they're in the wrong order.
- Merge - Uses the concept that merging two sorted list, you just need to compare the first values of the two lists and that a list of only one value is "sorted".
- Insertion - Take values from the unsorted list and insert it into a new sorted list at the correct position
- Quick sort - Choose a value on the list and split items bigger and smaller onto the two sides, that pivot is in the correct place. Repeat recursively with remaining values

**Searching Algorithms**

- Binary - Find the midpoint, compares to the value we want to find, removes the half of the list that is outside range.
- Linear - Iterates through every value
- Binary Tree - Uses binary tree logic to transverse the tree.
- Shortest Path

**Dijkstra’s algorithm** - Used to find the shortest path between nodes in a graph.  
A graph here means a set of objects known as nodes (or vertices). The nodes in the graph are connected by edges. These edges have a weighting – a value. The weights on the graph are a conceptual value and do not necessarily represent distance, the purpose of the shortest path algorithm is to reach a given goal node from a given start node

**Method**

- Calculate the distance of start node to all connected nodes.
- For each node, recursively calculate distance from each connected node to the start node, if it is less than currently recorded, record the length from start node and the previous node.

The CPU and main memory are valuable resources. There is a limit on what can be done in a given amount of time - When we design algorithms we want to be as efficient as possible. Big O Notation measures and compares the time & space complexity and performance of algorithms - It looks at how long an algorithm takes in the worst-case scenario.

_An “O” is used as a prefix for all expressions written in big O notation. An “n” is used to refer to the number of items. This is the algorithms “growth rate”, it can apply to both the time and space complexities._

**Complexities** - (where a represent constants & n is number of items.)

- Constant - O(a) - Complexity remains constant no matter how much data processed.
    - Performance doesn't change no matter the size of data.
    - A lot of algorithms have constant for their space complexity, as it doesn't take up any extra space even if there are more items in the array.
    - Hashed Random Access would be a valid example of this.
- Logarithmic - O(log n) - Complexity rises decreasingly as more data processed.
    - Binary search and binary tree search are examples of this.
    - For large data sets, these are much quicker than a linear search.
- Linear - O(n) - Complexity proportional to the number of items.
- Polynomial - O(nana) - Complexity rises increasingly as more data processed.
    - Examples of these would be functions that use nested loops like Nested + Bubble
- Exponential - O(anan) - Complexity rises exponentially as more data processed.  
    _In order of best to worst time complexity, on average._

Note that as n tends of infinity, coefficients of the notation becomes irrelevant. When considering the complexity of an algorithm, you need to consider each individual component and the complexity it adds.

text

```
for j in 0 to N:
    print(j)
```

This loop is going to happen N times. The loop itself has a growth rate of O(N). The line inside the loop is not dependant on the value of N, so it has a growth rate of O(1). Therefore complexity is N + 1. However, if we scale up the value of N towards infinity, the +1 becomes insignificant, so we can simplify it to O(N)

**Time Complexity** - Considering the extra space the algorithm uses as it is running

Bubble & insertion sort don’t create any new data structures or require more storage as they are running, so they are O(1)  
Merge sort requires the creation of N new lists when it is separating the elements out so that they can be merged back together, so has a space complexity of O(N)  
Quick sort is a recursive algorithm, so will be O(log(N)) for the same reasons as binary search.

Algorithms and programs (5)  
AS Computer Science  
Reduces a file by permanently eliminating  
certain information. When the file is  
uncompressed, only a part of the original  
information is still there. Generally used for  
video and audio files, where a certain amount  
of information loss will not be detected by most  
users.  
In compression of audio and video data, the  
final arbiter of quality, fidelity, or distortion is  
subjective and determined by the user.  
Involves checking whether actual results match  
expected results and that the software system  
is defect free. Testing is important because  
software bugs could be expensive to rectify or  
even dangerous to end users.  
Software testing aims to ensure:  
• cost-effectiveness  
• data security  
• product quality  
• customer satisfaction.  
Testing may be sub divided into three  
categories  
Functional testing: testing each function  
of the software application, by providing  
appropriate input and verifying the output  
against the functional requirements.  
Performance testing: testing the speed,  
response time, reliability, resource usage,  
scalability of a software program under  
expected workload.  
Regression testing: testing to confirm that  
program updates or that code changes have  
not adversely affected existing features.  
All data that was in the original file remains after  
the file is uncompressed. All of the information  
is completely restored. Generally used for text or  
spreadsheet files, where words or financial data  
must not be lost.  
The efficiency of a lossless algorithm can be  
assessed according to the amount of shrinkage of  
the source file, expressed as a ratio.  
compression ratio =  
size after compression  
size before compression  
OR as a percentage  
saving % = size before compression–size after compression  
size before compression  
%  
Unit testing: testing a section or unit of code in  
isolation to verify its correctness. A unit may be an  
individual function, method, procedure, or module.  
Integration Testing: testing to expose defects in  
the interaction between software units when they  
are integrated.  
System testing: testing of compiled software as  
a whole to check overall functionality, security etc.  
Acceptance Testing: formal testing conducted  
to determine whether a system satisfies its  
acceptance criteria and to enable the customer to  
determine whether to accept the system.  
Alpha Testing: a type of acceptance testing  
performed to identify bugs before releasing the  
product to real users or the public.  
Beta Testing: final testing before releasing  
application for commercial purpose, typically done  
by end-users.  
Whitebox testing: testing by the developers  
based on the inner workings of an application.  
Blackbox testing involves testing from an  
external or end-user perspective.  
The design of test data is crucial to the success of  
the testing process. Test data should include:  
Typical/standard data  
Data that is valid and should be processed  
correctly by the program, used to check validation  
rules and to confirm accuracy by comparing results  
with previously calculated outcomes, i.e. testing for  
logical errors.  
Extreme/boundary data  
Data that is valid but is at the extreme boundary  
of an acceptable range of values. Used to confirm  
data ranges have been correctly set.  
Erroneous data  
Data that would cause the program to fail if not  
validated and rejected.  
Lossy compression  
Software Testing  
Compression algorithms  
Software Testing strategies  
Test data  
Key terms  
Term Definition  
Compression The process of reducing file size.  
It allows more data to be stored  
on disk and enables files to be  
transferred more quickly.  
Testing  
strategy  
A strategy to ensure that a final  
solution to a problem meets the  
original requirements of the user.  
Test plan A document that details the  
scope, approach, resources,  
and schedule of intended test  
activities. Used to record the test  
data used and test outcomes.  
Lossless compression  
EXAMPLE  
Image source file of 256 x 256 bytes = 65536 bytes,  
compressed to 16384 bytes: -  
16384 / 65536 = ratio of 1 : 4 and  
a saving percentage = 75 %  
EXAMPLE  
A photograph of a blue sky would capture  
many variations in shades of blue. These can  
be replaced to use only the most frequently  
occurring shades and reference a much  
reduced set of colours.  
3.3 Algorithms and programs  
Learning objectives:  
•  
Understand the concept of “Big O notation” and how it can be used  
to determine the efficiency of algorithms  
To think about…  
•  
Why is it important to measure the efficiency of an algorithm?  
Computer Science  
Computational efficiency!  
Computational efficiency!  
The CPU and main memory are valuable resources. There is a limit on what  
The CPU and main memory are valuable resources. There is a limit on what can be done in a given amount of time. can be done in a given amount of time.  
Ideally when we design algorithms we want them to be as  
Ideally when we design algorithms we want them to be as efficient efficient as as possible to minimize impact of processing time and memory usage (space).possible to minimize impact of processing time and memory usage (space).  
Thereby freeing the CPU and main memory for other programs!  
Thereby freeing the CPU and main memory for other programs!  
Big O  
Big O NotationNotation  
Used to  
Used to measuremeasureand and comparecomparethe the time & space time & space complexitycomplexityand and performanceperformanceof algorithms. These things are dependant on how the algorithms of algorithms. These things are dependant on how the algorithms deal with amounts of deal with amounts of datadata. .  
Big O looks at how long an algorithm takes in the  
Big O looks at how long an algorithm takes in the worstworst--case scenariocase scenario. .  
For example, if you had an algorithm which searched through an array of 500 items,  
For example, if you had an algorithm which searched through an array of 500 items, big O would always assume you find the item of the 500big O would always assume you find the item of the 500ththsearch or not at all. search or not at all.  
An “O” is used as a prefix for all expressions written in big O notation.  
An “O” is used as a prefix for all expressions written in big O notation.  
An “n” is used to refer to the number of items.  
An “n” is used to refer to the number of items.  
For example an algorithm could have a complexity of O(  
For example an algorithm could have a complexity of O(𝑛3))  
This is said to be the algorithms “growth rate”. This can apply to both the time and  
This is said to be the algorithms “growth rate”. This can apply to both the time and space complexities. space complexities.  
Complexities  
Complexities  
•  
•There are 5 different “complexities” There are 5 different “complexities” or that you have to know aboutor that you have to know about  
•  
•Many common algorithms fit into Many common algorithms fit into one of these complexities, for time one of these complexities, for time as well as space considerations. as well as space considerations.  
•  
•For now, we’ll mainly focus on time For now, we’ll mainly focus on time complexity/performance growth rate complexity/performance growth rate ––that is, how the time taken to that is, how the time taken to complete the algorithm increases as complete the algorithm increases as the amount of data increasesthe amount of data increases  
Constant complexity  
Constant complexity  
Linear complexity  
Linear complexity  
Polynomial complexity  
Polynomial complexity  
Logarithmic complexity  
Logarithmic complexity  
Exponential complexity  
Exponential complexity  
Constant complexity  
Constant complexity  
•  
•Here, the complexity Here, the complexity remains the same no remains the same no matter how many data matter how many data items are processeditems are processed  
•  
•For example, finding the For example, finding the first item in a list will first item in a list will always take the same always take the same amount of time regardless amount of time regardless of the size of the list of the size of the list  
Constant complexity  
Constant complexity  
•  
•With constant complexity, the performance of the algorithm will never change no With constant complexity, the performance of the algorithm will never change no matter the size of the data matter the size of the data  
•  
•Other examples in programming include pushing and popping from a stack, and Other examples in programming include pushing and popping from a stack, and enqueingenqueingor or dequeingdequeingfrom a queue. It doesn’t matter size these structures are, each from a queue. It doesn’t matter size these structures are, each of these operations will require the same number of steps. of these operations will require the same number of steps.  
•  
•Hashing/direct/random access also fits into this category…why?Hashing/direct/random access also fits into this category…why?  
•  
•Many algorithms have O(1) for their space complexity, because the amount of extra Many algorithms have O(1) for their space complexity, because the amount of extra space the algorithm takes up when it’s running doesn’t increase no matter how space the algorithm takes up when it’s running doesn’t increase no matter how many items are in the array. We will see more of this later on. many items are in the array. We will see more of this later on.  
Constant complexity  
Constant complexity  
•  
•Consider this example. What is this Consider this example. What is this algorithm doing? algorithm doing?  
•  
•This algorithm using Modulus to This algorithm using Modulus to determine whether there is a determine whether there is a remainder after a divide by 2. If remainder after a divide by 2. If there is, the number must be oddthere is, the number must be odd  
•  
•It does not matter what size It does not matter what size number is entered into this number is entered into this algorithm, it will always take the algorithm, it will always take the same amount of timesame amount of time  
Linear complexity  
Linear complexity  
•  
•Here, the time complexity Here, the time complexity is directly proportional to is directly proportional to the number of items. Which the number of items. Which algorithm have we done algorithm have we done recently which has this for recently which has this for it’s time complexity?it’s time complexity?  
•  
•Linear search is an Linear search is an example of this, as every example of this, as every item in a list has to be item in a list has to be evaluated. evaluated.  
Linear complexity  
Linear complexity  
•  
•Examine this Examine this algorithm. Can you algorithm. Can you describe what it is describe what it is doing? doing?  
•  
•What else could we What else could we say of this algorithm? say of this algorithm?  
Linear complexity  
Linear complexity  
•  
•This algorithm will This algorithm will add the contents of a add the contents of a list list  
•  
•It is done recursively It is done recursively  
•  
•The more numbers in the list, the more recursive calls will be made and The more numbers in the list, the more recursive calls will be made and the longer it will take the longer it will take  
Polynomial complexity  
Polynomial complexity  
•  
•Here, the rate at which time Here, the rate at which time complexity rises increases as complexity rises increases as the number of items gets the number of items gets larger. Any ideas which larger. Any ideas which algorithms we’ve done recently algorithms we’ve done recently have this complexity?have this complexity?  
•  
•Bubble & insertion sorts are Bubble & insertion sorts are good examples of this, and any good examples of this, and any algorithm with nested loopsalgorithm with nested loops  
•  
•For example O(For example O(𝑛2) or O() or O(𝑛3))  
Logarithmic complexity  
Logarithmic complexity  
•  
•The increase in time The increase in time complexity decreases as the complexity decreases as the number of items increases. number of items increases. Which algorithms have we Which algorithms have we done recently have this done recently have this complexity?complexity?  
•  
•Binary search and binary Binary search and binary tree search are examples of tree search are examples of this. For large data sets, this. For large data sets, these are much quicker these are much quicker than a linear search. We than a linear search. We will examine this in more will examine this in more detail next lesson. detail next lesson.  
Exponential complexity  
Exponential complexity  
•  
•Here the time complexity Here the time complexity increase exponentially as increase exponentially as the number of items gets the number of items gets larger. larger.  
•  
•That is, the time That is, the time complexity of the complexity of the algorithm will double for algorithm will double for each additional data each additional data element. element.  
Exponential complexity  
Exponential complexity  
•  
•A recursive Fibonacci A recursive Fibonacci algorithm or an algorithm or an implementation of the implementation of the towers of Hanoi are good towers of Hanoi are good examples of thisexamples of this  
Order of time complexity  
Order of time complexity  
•  
•This is the order of This is the order of timetimecomplexity from complexity from best to worst performancebest to worst performance  
Calculating time complexity  
Calculating time complexity  
•  
•Given a simple algorithm such as the one on the Given a simple algorithm such as the one on the right it is possible to calculate the “growth rate” of right it is possible to calculate the “growth rate” of time complexity time complexity  
•  
•This loop is going to happen N times. The loop This loop is going to happen N times. The loop itself has a growth rate of O(N)itself has a growth rate of O(N)  
•  
•The line inside the loop is not dependant on the The line inside the loop is not dependant on the value of N, so it has a growth rate of O(1)value of N, so it has a growth rate of O(1)  
Calculating time complexity  
Calculating time complexity  
•  
•The complexity for this algorithm is therefore The complexity for this algorithm is therefore N + 1N + 1  
•  
•However, if we scale up the value of N However, if we scale up the value of N towards infinity, the +1 becomes insignificant, towards infinity, the +1 becomes insignificant, so we can simplify it to O(N)so we can simplify it to O(N)  
Calculating time complexity  
Calculating time complexity  
•  
•We’ve just seen that the inner loop will have We’ve just seen that the inner loop will have N+1 calculations (the OUTPUT line still isn’t N+1 calculations (the OUTPUT line still isn’t dependant on the value of N)dependant on the value of N)  
•  
•The outer loop happens N times so has a The outer loop happens N times so has a complexity of N complexity of N  
•  
•To find the time complexity for this whole To find the time complexity for this whole algorithm, we need to multiply everything algorithm, we need to multiply everything inside the nested loop by N, giving us inside the nested loop by N, giving us 𝑁2+N  
Calculating time complexity  
Calculating time complexity  
•  
•As before however, as the value of N tends As before however, as the value of N tends towards infinity the +N part becomes towards infinity the +N part becomes insignificantinsignificant  
•  
•So the final complexity for this algorithm is So the final complexity for this algorithm is O(O(𝑁2))  
Past paper question  
Past paper question ––20222022  
•  
•Attempt this past paper Attempt this past paper question in your notes question in your notes now. now.  
Past paper question  
Past paper question ––20222022  
Space complexity  
Space complexity  
•  
•Space complexity is different to time complexity Space complexity is different to time complexity --here we’re here we’re considering how much considering how much extraextraspace the algorithm will need to use as space the algorithm will need to use as it is running, rather than how long it will take. it is running, rather than how long it will take.  
•  
•You might assume that an algorithm dealing with N elements, like a You might assume that an algorithm dealing with N elements, like a linear search, would have a space complexity of O(N), but this isn’t the linear search, would have a space complexity of O(N), but this isn’t the case. The question to consider here is will the algorithm require more case. The question to consider here is will the algorithm require more space than it originally started with whilst it is running?space than it originally started with whilst it is running?  
Space complexity  
Space complexity  
•  
•With linear search, you keep track of the position you’re currently looking With linear search, you keep track of the position you’re currently looking at, and that’s it. No additional data will be needed as we go through the at, and that’s it. No additional data will be needed as we go through the algorithm, so the space complexity of linear search is O(1).algorithm, so the space complexity of linear search is O(1).  
•  
•What about binary search? What about binary search?  
•  
•This actually depends on the implementation. If it’s done with an iterative This actually depends on the implementation. If it’s done with an iterative (while loop) approach, then it will be O(1) as well, because it will not use (while loop) approach, then it will be O(1) as well, because it will not use more space during runtime. If it is recursive however, it will require new a more space during runtime. If it is recursive however, it will require new a new function call with its own variables & return address every time it new function call with its own variables & return address every time it calls itself, so it would have a space complexity of O(log(N)). calls itself, so it would have a space complexity of O(log(N)).  
Efficiency of sorting algorithms  
Efficiency of sorting algorithms  
•  
•Consider the diagram on the Consider the diagram on the time and space efficiency of time and space efficiency of sorting algorithms here. sorting algorithms here.  
•  
•With the person next to you, can With the person next to you, can you give reasons for the space you give reasons for the space complexity of the main 4 we’ve complexity of the main 4 we’ve learned about learned about ––bubble, quick, bubble, quick, insertion & merge?insertion & merge?  
Space complexity for sorting algorithms  
Space complexity for sorting algorithms  
•  
•Bubble & insertion sort don’t create any new data structures or Bubble & insertion sort don’t create any new data structures or require more storage as they are running, so they are O(1)require more storage as they are running, so they are O(1)  
•  
•Merge sort requires the creation of N new lists when it is separating Merge sort requires the creation of N new lists when it is separating the elements out so that they can be merged back together, so has a the elements out so that they can be merged back together, so has a space complexity of O(N)space complexity of O(N)  
•  
•Quicksort is a recursive algorithm, so will be O(log(N)) for the same Quicksort is a recursive algorithm, so will be O(log(N)) for the same reasons as binary search. reasons as binary search.

3.3 Algorithms and programs  
Learning objectives:  
•  
Examine the use of different sorting and searching algorithms  
•  
Confidently identify applications of the algorithms and describe algorithm  
efficiency  
To think about…  
•  
What sorting and searching algorithms did you learn about last year? Can you  
describe them in detail?  
Computer Science  
Sorting algorithms  
Sorting algorithms  
Quicksort  
Quicksort  
Insertion  
Insertion sortsort  
Bubble sort  
Bubble sort  
Merge sort  
Merge sort  
Searching algorithms  
Searching algorithms  
Linear/serial  
Linear/serial searchsearch  
Binary  
Binary searchsearch  
Binary tree  
Binary tree searchsearch  
Shortest  
Shortest--pathpath  
Bubble sort  
Bubble sort  
•  
•Compares pairs of Compares pairs of elements in the list 1 elements in the list 1 at a time (0 and 1, 1 at a time (0 and 1, 1 and 2, 2 and 3 etc.)and 2, 2 and 3 etc.)  
•  
•Swaps elements as it Swaps elements as it goes where necessarygoes where necessary  
•  
•Potentially requires Potentially requires multiple passes multiple passes through data setthrough data set  
Merge sort  
Merge sort  
•  
•Splitting a list of n Splitting a list of n items into n lists items into n lists  
•  
•Data/lists sorted in Data/lists sorted in turn into the correct turn into the correct orderorder  
Insertion sort  
Insertion sort  
•  
•List split into sorted List split into sorted and unsorted portionsand unsorted portions  
•  
•Numbers “inserted” to Numbers “inserted” to the sorted portion the sorted portion one at a time, one at a time, comparing with other comparing with other elements in sorted elements in sorted portion. portion.  
•  
•Similar to but Similar to but generally better than generally better than bubblebubble  
Quicksort  
Quicksort  
•  
•A recursive algorithmA recursive algorithm  
•  
•Uses pivots and Uses pivots and pointers pointers  
Sorting algorithms  
Sorting algorithms  
Linear search  
Linear search  
•  
•Searches through Searches through each element in each element in the list one after the list one after the otherthe other  
•  
•Data can be in Data can be in any orderany order  
Binary search  
Binary search  
•  
•Middle point taken, Middle point taken, if the number is if the number is lower, discard any lower, discard any data above (and data above (and vice versa) and vice versa) and repeat. repeat.  
•  
•This reduces the This reduces the target area by half target area by half each timeeach time  
•  
•Data Data must be must be sorted sorted  
Binary tree search  
Binary tree search  
•  
•Compare search Compare search item with current item with current nodenode  
•  
•Travel down Travel down branches in the branches in the tree until correct tree until correct node is foundnode is found  
•  
•Data Data must be in must be in order order in the tree in the tree  
Shortest  
Shortest--pathpath  
•  
•Main algorithms Main algorithms are are Dijkstra’sDijkstra’sand and A_A_  
•  
•Used to find Used to find shortest paths on shortest paths on a graph/grida graph/grid  
•  
•Used in games for Used in games for AI pathfindingAI pathfinding  
Searching algorithms  
Searching algorithms  
Insertion sort  
Insertion sort --[https://www.youtube.com/watch?v=DFGhttps://www.youtube.com/watch?v=DFG--XuyPYUQXuyPYUQ](https://www.youtube.com/watch?v=DFGhttps://www.youtube.com/watch?v=DFG--XuyPYUQXuyPYUQ)  
Bubble sort  
Bubble sort --[https://www.youtube.com/watch?v=8Kphttps://www.youtube.com/watch?v=8Kp--8OGwphY8OGwphY](https://www.youtube.com/watch?v=8Kphttps://www.youtube.com/watch?v=8Kp--8OGwphY8OGwphY)  
Quick sort  
Quick sort --[https://www.youtube.com/watch?v=Hoixgm4https://www.youtube.com/watch?v=Hoixgm4--P4MP4M](https://www.youtube.com/watch?v=Hoixgm4https://www.youtube.com/watch?v=Hoixgm4--P4MP4M)  
Alternative quick sort  
Alternative quick sort --[https://www.youtube.com/watch?v=aXXWXz5rF64https://www.youtube.com/watch?v=aXXWXz5rF64](https://www.youtube.com/watch?v=aXXWXz5rF64https://www.youtube.com/watch?v=aXXWXz5rF64)  
Merge sort  
Merge sort --[https://www.youtube.com/watch?v=EeQ8pwjQxTMhttps://www.youtube.com/watch?v=EeQ8pwjQxTM](https://www.youtube.com/watch?v=EeQ8pwjQxTMhttps://www.youtube.com/watch?v=EeQ8pwjQxTM)  
Quick sort vs merge sort  
Quick sort vs merge sort --[https://www.youtube.com/watch?v=es2T6KY45cAhttps://www.youtube.com/watch?v=es2T6KY45cA](https://www.youtube.com/watch?v=es2T6KY45cAhttps://www.youtube.com/watch?v=es2T6KY45cA)  
Linear search (aka serial search)  
Linear search (aka serial search)  
•  
•This is by far the easiest of the search algorithms. You might have already This is by far the easiest of the search algorithms. You might have already implemented one of these implemented one of these  
•  
•Each element checked in order to see if it matches the search term Each element checked in order to see if it matches the search term  
Binary search